name: Code Scanning

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: "17 6 * * 1"   # weekly — Monday 06:17 UTC
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  rexxlint:
    name: rexxlint SARIF upload
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Build rexxlint
        run: cargo build --release -p rexx-cli

      - name: Run rexxlint and emit SARIF
        # Exit 1 = findings (expected); exit 2 = hard error (propagate).
        run: |
          ./target/release/rexxlint check --output sarif . > rexxlint.sarif
        continue-on-error: true

      - name: Upload SARIF to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: rexxlint.sarif
          category: rexxlint
